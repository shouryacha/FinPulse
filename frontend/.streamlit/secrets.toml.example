# Copy this file to secrets.toml for local development (secrets.toml is gitignored).
# On Streamlit Community Cloud, set the same key under App settings -> Secrets instead.

BACKEND_URL = "http://127.0.0.1:8000"
